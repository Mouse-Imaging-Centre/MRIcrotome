% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeFuncs.R
\name{maskBounds}
\alias{maskBounds}
\title{Compute bounding box of a mask volume}
\usage{
maskBounds(mask, dimension, padding = 10)
}
\arguments{
\item{mask}{A 3D array where non-zero values indicate the region of interest.}

\item{dimension}{The dimension (1, 2, or 3) along which to compute bounds.}

\item{padding}{Number of voxels to add as padding on each side. Defaults to 10.}
}
\value{
A named numeric vector with elements \code{begin} and \code{end}.
}
\description{
Given a binary mask, returns the begin and end slices along the
specified dimension, with optional padding. Useful for setting the
\code{begin} and \code{end} arguments of \code{\link{sliceSeries}}.
}
\examples{
\dontrun{
bounds <- maskBounds(binaryMask, dimension = 2, padding = 10)
sliceSeries(nrow = 5, begin = bounds["begin"], end = bounds["end"],
            dimension = 2) \\%>\\% ...
}
}
